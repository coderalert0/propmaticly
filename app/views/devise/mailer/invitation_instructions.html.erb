<%= image_tag('logo.png', height: '50', alt: 'Propmaticly Logo') %>

<p>
  <%= t("devise.mailer.invitation_instructions.someone_invited_you",
        inviter: "#{@resource.invited_by&.first_name} #{@resource.invited_by&.last_name}" || "Someone") %>
</p>

<%= link_to t("devise.mailer.invitation_instructions.accept"), accept_invitation_url(@resource, invitation_token: @token) %>

<% if @resource.invitation_due_at %>
  <p>
    <%= t("devise.mailer.invitation_instructions.accept_until",
          due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
  </p>
<% end %>

<p>If the button above doesn’t work, please copy and paste the following URL into your browser:</p>
<p><%= accept_invitation_url(@resource, invitation_token: @token) %></p>

<p><%= t("devise.mailer.invitation_instructions.ignore") %></p>

<p>Thank you,<br>Propmaticly Team</p>
