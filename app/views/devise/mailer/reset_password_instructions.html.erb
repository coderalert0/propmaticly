<%= image_tag('logo.png', height: '50', alt: 'Propmaticly Logo') %>

<p>We received a request to reset your password. To proceed, please click the button below:</p>

<%= link_to 'Reset My Password', edit_password_url(@resource, reset_password_token: @token) %>


<p>If the button above doesn’t work, copy and paste the following link into your browser:</p>
<p><%= edit_password_url(@resource, reset_password_token: @token) %></p>

<p>If you did not request a password reset, please ignore this email or contact our support team at <a href="mailto:support@propmaticly.com">support@propmaticly.com</a>.</p>

<p>Your password will remain unchanged until you use the link above to create a new one.</p>

<p>Thank you,<br>The Propmaticly Team</p>
