<div class="modal fade modal-lg" id="editUserProfileModal<%= user.id %>" tabindex="-1" aria-hidden="true" data-controller="modal">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Edit User</h5>
        <button type="button" class="btn-close" data-action="modal#close" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div id="error-<%= user.id %>"></div>

        <!--begin::Form-->
        <%= form_for(user, url: user_profile_path(user), remote: true) do |f| %>
          <%= render partial: 'user_profiles/edit_form', locals: { f: f } %>
        <% end %>
        <!--end::Form-->
      </div>
    </div>
  </div>
</div>
