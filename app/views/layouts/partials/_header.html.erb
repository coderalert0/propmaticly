<nav class="navbar navbar-expand-lg">
  <div class="container-fluid p-0 align-items-end">
    <!-- Left-aligned items -->
    <ul class="navbar-nav align-items-end">
      <li><%= link_to image_tag('logo.png', size: '100x50', alt: 'MyApp Logo'), root_path, class: 'nav-link me-10' %></li>
      <% if user_signed_in? %>
        <li><h4><%= link_to "Dashboard", root_path, class: 'me-10' %></h4></li>
        <% if current_user&.admin? %>
          <li><h4><%= link_to "Users", users_path, class: 'me-10'  %></h4></li>
        <% end %>
        <li><h4><%= link_to "Inspections", root_path %></h4></li>
      <% end %>
    </ul>

    <!-- Right-aligned item -->
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <span class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
            Hello <%= current_user&.first_name %>
          </span>
          <div class="dropdown-menu p-5">
            <div>
              <!--begin::Username-->
              <div class="fw-bold">
                <%= current_user&.first_name %> <%= current_user&.last_name %>
                <span class="bg-success fw-bold px-2 py-1 ms-2 text-white">Pro</span>
              </div>
              <div class="text-muted">
                <%= current_user&.email %>
              </div>
              <!--end::Username-->
            </div>
            <!--end::Menu item-->

            <!--begin::Menu separator-->
            <div class="dropdown-divider my-2"></div>
            <!--end::Menu separator-->

            <!--begin::Menu item-->
            <a href="#" class="dropdown-item">My Subscription</a>
            <!--end::Menu item-->

            <!--begin::Menu separator-->
            <div class="dropdown-divider"></div>
            <!--end::Menu separator-->

            <!--begin::Menu item-->
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            <!--end::Menu item-->
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

